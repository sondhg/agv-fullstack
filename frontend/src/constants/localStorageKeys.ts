export const HTTP_OR_MQTT_TAB_KEY = "http-or-mqtt-tab"; // Key for storing the active tab in localStorage
export const CACHED_MAP_DATA_KEY = "cachedMapData"; // Key for caching map data in localStorage
export const SIMULATION_STEP_KEY = "agv-simulation-current-step"; // Key for storing the simulation step index in localStorage
export const ACCESS_TOKEN_KEY = "access_token"; // Key for storing the access token in localStorage
export const VITE_UI_THEME_KEY = "vite-ui-theme"; // Key for storing the UI theme in localStorage
